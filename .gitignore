.mypy_cache/
.ruff_cache/
__pycache__/
dist/
poetry.toml

# Local secrets and virtualenvs
.env
.envrc
.venv/

# Fern generation metadata (records commit/dirty/invokedBy — not reproducible)
.fern/

# Stray uv lockfile (this project uses poetry.lock)
uv.lock
