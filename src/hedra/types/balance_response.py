# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class BalanceResponse(UniversalBaseModel):
    """
    Spendable balance on the account this key bills.

    Sourced from the workspace credit pool (the source of truth) for a
    workspace-bound key, else the caller's personal ledger balance. While the
    segregated API wallet is enabled, both key types instead read the API wallet
    used by generation debits (a personal key resolves its bound workspace).

    ``credits`` remains the stable display unit across the USD cutover. ``usd``
    is the display-friendly dollar balance and ``usd_micros`` is the exact
    integer micro-dollar balance while either USD billing or the natively-USD
    API wallet is enabled; both are omitted before then so the flag-off wire
    response is unchanged.
    """

    credits: int
    usd: typing.Optional[float] = pydantic.Field(default=None)
    """
    Spendable balance in USD, derived from usd_micros.
    """

    usd_micros: typing.Optional[int] = pydantic.Field(default=None)
    """
    Exact spendable balance in integer micro-dollars (1e-6 USD).
    """

    allocated: typing.Optional[int] = None
    used: typing.Optional[int] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
