# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.input_creatify_aurora import InputCreatifyAurora
from ..types.input_dreamina31 import InputDreamina31
from ..types.input_elevenlabs_audio_isolation import InputElevenlabsAudioIsolation
from ..types.input_elevenlabs_english_sts_v2 import InputElevenlabsEnglishStsV2
from ..types.input_elevenlabs_flash_multilingual_v2 import InputElevenlabsFlashMultilingualV2
from ..types.input_elevenlabs_flash_v2 import InputElevenlabsFlashV2
from ..types.input_elevenlabs_multilingual_sts_v2 import InputElevenlabsMultilingualStsV2
from ..types.input_elevenlabs_multilingual_v2 import InputElevenlabsMultilingualV2
from ..types.input_elevenlabs_music import InputElevenlabsMusic
from ..types.input_elevenlabs_sound_effects import InputElevenlabsSoundEffects
from ..types.input_elevenlabs_v3 import InputElevenlabsV3
from ..types.input_elevenlabs_voice_clone import InputElevenlabsVoiceClone
from ..types.input_flux2flex import InputFlux2Flex
from ..types.input_flux2klein9b import InputFlux2Klein9B
from ..types.input_flux2max import InputFlux2Max
from ..types.input_flux2pro import InputFlux2Pro
from ..types.input_flux3 import InputFlux3
from ..types.input_flux11pro import InputFlux11Pro
from ..types.input_flux11ultra import InputFlux11Ultra
from ..types.input_flux_dev import InputFluxDev
from ..types.input_flux_kontext_max import InputFluxKontextMax
from ..types.input_flux_kontext_pro import InputFluxKontextPro
from ..types.input_gemini_omni_flash import InputGeminiOmniFlash
from ..types.input_gpt_image2 import InputGptImage2
from ..types.input_gpt_image15 import InputGptImage15
from ..types.input_grok_imagine import InputGrokImagine
from ..types.input_grok_imagine20 import InputGrokImagine20
from ..types.input_grok_video import InputGrokVideo
from ..types.input_happy_horse import InputHappyHorse
from ..types.input_hedra_avatar import InputHedraAvatar
from ..types.input_hedra_character3 import InputHedraCharacter3
from ..types.input_heygen_photo_avatar4 import InputHeygenPhotoAvatar4
from ..types.input_hidream_o1image import InputHidreamO1Image
from ..types.input_ideogram_v2 import InputIdeogramV2
from ..types.input_ideogram_v4 import InputIdeogramV4
from ..types.input_imagen3 import InputImagen3
from ..types.input_imagen4 import InputImagen4
from ..types.input_kling16 import InputKling16
from ..types.input_kling21master import InputKling21Master
from ..types.input_kling25turbo import InputKling25Turbo
from ..types.input_kling26motion_control import InputKling26MotionControl
from ..types.input_kling26pro import InputKling26Pro
from ..types.input_kling_ai_avatar_v2 import InputKlingAiAvatarV2
from ..types.input_kling_o1 import InputKlingO1
from ..types.input_kling_o3 import InputKlingO3
from ..types.input_kling_o3edit import InputKlingO3Edit
from ..types.input_kling_o3reference import InputKlingO3Reference
from ..types.input_kling_v3 import InputKlingV3
from ..types.input_kling_v3motion_control import InputKlingV3MotionControl
from ..types.input_ltx23 import InputLtx23
from ..types.input_luma_ray32 import InputLumaRay32
from ..types.input_mai_image25 import InputMaiImage25
from ..types.input_minimax_h3 import InputMinimaxH3
from ..types.input_minimax_hailuo02 import InputMinimaxHailuo02
from ..types.input_minimax_hailuo23 import InputMinimaxHailuo23
from ..types.input_minimax_speech25hd_preview import InputMinimaxSpeech25HdPreview
from ..types.input_minimax_speech25turbo_preview import InputMinimaxSpeech25TurboPreview
from ..types.input_nano_banana import InputNanoBanana
from ..types.input_nano_banana2 import InputNanoBanana2
from ..types.input_nano_banana_pro import InputNanoBananaPro
from ..types.input_omnihuman15 import InputOmnihuman15
from ..types.input_pixverse_v6 import InputPixverseV6
from ..types.input_qwen_image2 import InputQwenImage2
from ..types.input_recraft_v3 import InputRecraftV3
from ..types.input_reve21 import InputReve21
from ..types.input_reve21edit import InputReve21Edit
from ..types.input_reve21remix import InputReve21Remix
from ..types.input_sana import InputSana
from ..types.input_seedance15pro import InputSeedance15Pro
from ..types.input_seedance20 import InputSeedance20
from ..types.input_seedance20mini import InputSeedance20Mini
from ..types.input_seedance25 import InputSeedance25
from ..types.input_seedream40 import InputSeedream40
from ..types.input_seedream45 import InputSeedream45
from ..types.input_seedream50lite import InputSeedream50Lite
from ..types.input_seedream50pro import InputSeedream50Pro
from ..types.input_sora2pro import InputSora2Pro
from ..types.input_topaz_image_upscaler import InputTopazImageUpscaler
from ..types.input_topaz_image_upscaler_wonder import InputTopazImageUpscalerWonder
from ..types.input_topaz_video_upscaler import InputTopazVideoUpscaler
from ..types.input_topaz_video_upscaler_hyperion25 import InputTopazVideoUpscalerHyperion25
from ..types.input_topaz_video_upscaler_starlight_fast import InputTopazVideoUpscalerStarlightFast
from ..types.input_topaz_video_upscaler_starlight_hq import InputTopazVideoUpscalerStarlightHq
from ..types.input_topaz_video_upscaler_starlight_precise import InputTopazVideoUpscalerStarlightPrecise
from ..types.input_veed_fabric10 import InputVeedFabric10
from ..types.input_veed_video_background_removal import InputVeedVideoBackgroundRemoval
from ..types.input_veo2 import InputVeo2
from ..types.input_veo3 import InputVeo3
from ..types.input_veo31 import InputVeo31
from ..types.input_vidu_q3 import InputViduQ3
from ..types.input_vidu_q3reference import InputViduQ3Reference
from ..types.input_wan27 import InputWan27
from ..types.input_wan30 import InputWan30
from ..types.job_list_response import JobListResponse
from ..types.job_log_item import JobLogItem
from ..types.job_log_list_response import JobLogListResponse
from ..types.job_summary import JobSummary
from ..types.result_response import ResultResponse
from ..types.status_response import StatusResponse
from ..types.submit_response import SubmitResponse
from .raw_client import AsyncRawJobsClient, RawJobsClient
from .types.jobs_stream_response import JobsStreamResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class JobsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawJobsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawJobsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawJobsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[JobSummary, JobListResponse]:
        """
        Parameters
        ----------
        limit : typing.Optional[int]
            Maximum items per page.

        cursor : typing.Optional[str]
            Opaque cursor from the previous page's `next_cursor`; omit for the first page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[JobSummary, JobListResponse]
            Successful Response

        Examples
        --------
        from hedra import Hedra

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        response = client.jobs.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(limit=limit, cursor=cursor, request_options=request_options)

    def get(self, job_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> ResultResponse:
        """
        Parameters
        ----------
        job_id : str
            The job's id (`job_<uuid>`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResultResponse
            Successful Response

        Examples
        --------
        from hedra import Hedra

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.get(
            job_id="job_id",
        )
        """
        _response = self._raw_client.get(job_id, request_options=request_options)
        return _response.data

    def get_status(
        self,
        job_id: str,
        *,
        logs_after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StatusResponse:
        """
        Parameters
        ----------
        job_id : str
            The job's id (`job_<uuid>`).

        logs_after : typing.Optional[str]
            Tail this job's lifecycle events incrementally: returns only events newer than this cursor, plus `logs_next_cursor` to send on the next poll. Pass `start` to begin from the job's first event. Omit it and the response carries no events at all — the default polling shape is unchanged.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StatusResponse
            Successful Response

        Examples
        --------
        from hedra import Hedra

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.get_status(
            job_id="job_id",
        )
        """
        _response = self._raw_client.get_status(job_id, logs_after=logs_after, request_options=request_options)
        return _response.data

    def list_job_logs(
        self,
        job_id: str,
        *,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[JobLogItem, JobLogListResponse]:
        """
        Parameters
        ----------
        job_id : str
            The job's id (`job_<uuid>`).

        limit : typing.Optional[int]
            Maximum items per page.

        cursor : typing.Optional[str]
            Opaque cursor from the previous page's `next_cursor`; omit for the first page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[JobLogItem, JobLogListResponse]
            Successful Response

        Examples
        --------
        from hedra import Hedra

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        response = client.jobs.list_job_logs(
            job_id="job_id",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list_job_logs(job_id, limit=limit, cursor=cursor, request_options=request_options)

    def stream(
        self,
        job_id: str,
        *,
        last_event_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Iterator[JobsStreamResponse]:
        """
        Parameters
        ----------
        job_id : str
            The job's id (`job_<uuid>`).

        last_event_id : typing.Optional[str]
            Resume after this event id — the standard SSE reconnect header; browsers' EventSource sends it automatically.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Yields
        ------
        typing.Iterator[JobsStreamResponse]
            Server-Sent Events. Each frame's `event:` names its type. The `data:` line of a `status` frame is a `StatusResponse` and of a `log` frame a `JobLogItem` (the same row `GET /jobs/{job_id}/logs` serves), both JSON. The stream opens with a `status` snapshot and ends with a `done` frame, sent after the terminal `status` frame: reaching it means the job finished (`COMPLETED` or `FAILED`) and there is nothing more to read. A stream that ends without it was cut short; reconnect with `Last-Event-Id`. Closes the server initiates instead (drain, max connection duration) send a `retry:` line and no `done`. **The `done` frame's `data:` line is the literal token `[STREAM_DONE]` — not JSON, and not a member of the `JobStreamFrame` schema below.** It is a fixed end-of-stream sentinel, matched verbatim; switch on `event:` and skip `done` before parsing a `data:` line, or the parse fails on the last frame of every stream. It repeats no state: which job ended and how was just delivered by the terminal `status` frame. `status` and `log` frames carry an `id:` to send back as `Last-Event-Id`; `done` does not, since it is not a new stream position. Comment lines (`: keep-alive`) and `retry:` lines are protocol-level and carry no payload.

        Examples
        --------
        from hedra import Hedra

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        response = client.jobs.stream(
            job_id="job_id",
        )
        for chunk in response:
            yield chunk
        """
        with self._raw_client.stream(job_id, last_event_id=last_event_id, request_options=request_options) as r:
            yield from r.data

    def submit_creatify_aurora(
        self,
        *,
        input: InputCreatifyAurora,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Create high-fidelity speaking or singing avatar videos.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputCreatifyAurora

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputCreatifyAurora,
            InputCreatifyAuroraAudio_Url,
            InputCreatifyAuroraStartImage_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_creatify_aurora(
            input=InputCreatifyAurora(
                resolution="480p",
                start_image=InputCreatifyAuroraStartImage_Url(
                    url="url",
                ),
                audio=InputCreatifyAuroraAudio_Url(
                    url="url",
                ),
            ),
        )
        """
        _response = self._raw_client.submit_creatify_aurora(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_dreamina31(
        self,
        *,
        input: InputDreamina31,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Polished, print-ready stills when the brief is a finished image rather than a sketch.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputDreamina31

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputDreamina31

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_dreamina31(
            input=InputDreamina31(
                prompt="prompt",
                aspect_ratio="16:9",
                resolution="540p",
            ),
        )
        """
        _response = self._raw_client.submit_dreamina31(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_elevenlabs_audio_isolation(
        self,
        *,
        input: InputElevenlabsAudioIsolation,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Strip background noise from a recording, keeping the speech.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsAudioIsolation

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputElevenlabsAudioIsolation,
            InputElevenlabsAudioIsolationAudio_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_elevenlabs_audio_isolation(
            input=InputElevenlabsAudioIsolation(
                audio=InputElevenlabsAudioIsolationAudio_Url(
                    url="url",
                ),
            ),
        )
        """
        _response = self._raw_client.submit_elevenlabs_audio_isolation(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_elevenlabs_english_sts_v2(
        self,
        *,
        input: InputElevenlabsEnglishStsV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsEnglishStsV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputElevenlabsEnglishStsV2,
            InputElevenlabsEnglishStsV2Audio_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_elevenlabs_english_sts_v2(
            input=InputElevenlabsEnglishStsV2(
                audio=InputElevenlabsEnglishStsV2Audio_Url(
                    url="url",
                ),
                voice_id="voice_id",
            ),
        )
        """
        _response = self._raw_client.submit_elevenlabs_english_sts_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_elevenlabs_flash_multilingual_v2(
        self,
        *,
        input: InputElevenlabsFlashMultilingualV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The low-latency voice across 30+ languages, for interactive and high-volume speech.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsFlashMultilingualV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputElevenlabsFlashMultilingualV2

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_elevenlabs_flash_multilingual_v2(
            input=InputElevenlabsFlashMultilingualV2(
                text="text",
                voice_id="voice_id",
            ),
        )
        """
        _response = self._raw_client.submit_elevenlabs_flash_multilingual_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_elevenlabs_flash_v2(
        self,
        *,
        input: InputElevenlabsFlashV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The low-latency English voice, for interactive speech.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsFlashV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputElevenlabsFlashV2

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_elevenlabs_flash_v2(
            input=InputElevenlabsFlashV2(
                text="text",
                voice_id="voice_id",
            ),
        )
        """
        _response = self._raw_client.submit_elevenlabs_flash_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_elevenlabs_multilingual_sts_v2(
        self,
        *,
        input: InputElevenlabsMultilingualStsV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsMultilingualStsV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputElevenlabsMultilingualStsV2,
            InputElevenlabsMultilingualStsV2Audio_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_elevenlabs_multilingual_sts_v2(
            input=InputElevenlabsMultilingualStsV2(
                audio=InputElevenlabsMultilingualStsV2Audio_Url(
                    url="url",
                ),
                voice_id="voice_id",
            ),
        )
        """
        _response = self._raw_client.submit_elevenlabs_multilingual_sts_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_elevenlabs_multilingual_v2(
        self,
        *,
        input: InputElevenlabsMultilingualV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Steady, natural narration across 30+ languages, for finished voiceover.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsMultilingualV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputElevenlabsMultilingualV2

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_elevenlabs_multilingual_v2(
            input=InputElevenlabsMultilingualV2(
                text="text",
                voice_id="voice_id",
            ),
        )
        """
        _response = self._raw_client.submit_elevenlabs_multilingual_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_elevenlabs_music(
        self,
        *,
        input: InputElevenlabsMusic,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Full tracks from a written brief, with optional lyrics placed across the length you ask for.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsMusic

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputElevenlabsMusic

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_elevenlabs_music(
            input=InputElevenlabsMusic(
                prompt="prompt",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_elevenlabs_music(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_elevenlabs_sound_effects(
        self,
        *,
        input: InputElevenlabsSoundEffects,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        One-off sound effects from a written description, loopable on request.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsSoundEffects

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputElevenlabsSoundEffects

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_elevenlabs_sound_effects(
            input=InputElevenlabsSoundEffects(
                text="text",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_elevenlabs_sound_effects(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_elevenlabs_v3(
        self,
        *,
        input: InputElevenlabsV3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The most expressive ElevenLabs voice — emotional range and delivery cues for performance, not just narration.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsV3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputElevenlabsV3

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_elevenlabs_v3(
            input=InputElevenlabsV3(
                text="text",
                voice_id="voice_id",
            ),
        )
        """
        _response = self._raw_client.submit_elevenlabs_v3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_elevenlabs_voice_clone(
        self,
        *,
        input: InputElevenlabsVoiceClone,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Use an audio clip to create a new Voice.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsVoiceClone

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputElevenlabsVoiceClone,
            InputElevenlabsVoiceCloneAudio_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_elevenlabs_voice_clone(
            input=InputElevenlabsVoiceClone(
                audio=InputElevenlabsVoiceCloneAudio_Url(
                    url="url",
                ),
                name="name",
            ),
        )
        """
        _response = self._raw_client.submit_elevenlabs_voice_clone(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_flux11pro(
        self,
        *,
        input: InputFlux11Pro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Premium color depth and clarity when you want campaign-ready art that feels handcrafted.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux11Pro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputFlux11Pro

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_flux11pro(
            input=InputFlux11Pro(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="540p",
            ),
        )
        """
        _response = self._raw_client.submit_flux11pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_flux11ultra(
        self,
        *,
        input: InputFlux11Ultra,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The big-canvas choice for ultra-high-res images and flagship visuals.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux11Ultra

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputFlux11Ultra

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_flux11ultra(
            input=InputFlux11Ultra(
                prompt="prompt",
                aspect_ratio="16:9",
            ),
        )
        """
        _response = self._raw_client.submit_flux11ultra(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_flux3(
        self,
        *,
        input: InputFlux3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Video with native audio, straight from a prompt.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputFlux3

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_flux3(
            input=InputFlux3(
                prompt="prompt",
                aspect_ratio="auto",
                resolution="720p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_flux3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_flux_dev(
        self,
        *,
        input: InputFluxDev,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Fast and light for quick concepts or high-volume social posts on a budget.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFluxDev

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputFluxDev

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_flux_dev(
            input=InputFluxDev(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="540p",
            ),
        )
        """
        _response = self._raw_client.submit_flux_dev(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_flux_kontext_max(
        self,
        *,
        input: InputFluxKontextMax,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Highest-fidelity reference-image support for complex, multi-element scenes and perfectly matched branded visuals.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFluxKontextMax

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputFluxKontextMax

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_flux_kontext_max(
            input=InputFluxKontextMax(
                prompt="prompt",
            ),
        )
        """
        _response = self._raw_client.submit_flux_kontext_max(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_flux_kontext_pro(
        self,
        *,
        input: InputFluxKontextPro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Reference-image support for character, brand, or style consistency.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFluxKontextPro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputFluxKontextPro

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_flux_kontext_pro(
            input=InputFluxKontextPro(
                prompt="prompt",
            ),
        )
        """
        _response = self._raw_client.submit_flux_kontext_pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_flux2flex(
        self,
        *,
        input: InputFlux2Flex,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The tunable Flux.2 tier — trade denoising steps against speed per generation.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux2Flex

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputFlux2Flex

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_flux2flex(
            input=InputFlux2Flex(
                prompt="prompt",
                aspect_ratio="16:9",
            ),
        )
        """
        _response = self._raw_client.submit_flux2flex(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_flux2klein9b(
        self,
        *,
        input: InputFlux2Klein9B,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The lean Flux.2 tier — quick, inexpensive stills for concepting and high-volume work.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux2Klein9B

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputFlux2Klein9B

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_flux2klein9b(
            input=InputFlux2Klein9B(
                prompt="prompt",
                aspect_ratio="16:9",
            ),
        )
        """
        _response = self._raw_client.submit_flux2klein9b(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_flux2max(
        self,
        *,
        input: InputFlux2Max,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The top Flux.2 tier, for realism and precision in final deliverables.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux2Max

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputFlux2Max

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_flux2max(
            input=InputFlux2Max(
                prompt="prompt",
                aspect_ratio="16:9",
            ),
        )
        """
        _response = self._raw_client.submit_flux2max(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_flux2pro(
        self,
        *,
        input: InputFlux2Pro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The everyday Flux.2 tier — style transfer and sequential edits that hold together across passes.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux2Pro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputFlux2Pro

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_flux2pro(
            input=InputFlux2Pro(
                prompt="prompt",
                aspect_ratio="16:9",
            ),
        )
        """
        _response = self._raw_client.submit_flux2pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_gemini_omni_flash(
        self,
        *,
        input: InputGeminiOmniFlash,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Gemini's fast multimodal video model — cinematic clips with native audio from a prompt, a keyframe, or reference images.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputGeminiOmniFlash

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputGeminiOmniFlash

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_gemini_omni_flash(
            input=InputGeminiOmniFlash(
                prompt="prompt",
                aspect_ratio="16:9",
            ),
        )
        """
        _response = self._raw_client.submit_gemini_omni_flash(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_gpt_image15(
        self,
        *,
        input: InputGptImage15,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Reads a long, specific brief closely — the choice when the prompt carries the detail.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputGptImage15

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputGptImage15

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_gpt_image15(
            input=InputGptImage15(
                prompt="prompt",
                aspect_ratio="1:1",
            ),
        )
        """
        _response = self._raw_client.submit_gpt_image15(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_gpt_image2(
        self,
        *,
        input: InputGptImage2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        OpenAI's balanced tier; moderate cost and fidelity, ideal for iterative refinement and everyday generation.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputGptImage2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputGptImage2

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_gpt_image2(
            input=InputGptImage2(
                prompt="prompt",
                aspect_ratio="16:9",
                resolution="1K",
            ),
        )
        """
        _response = self._raw_client.submit_gpt_image2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_grok_imagine(
        self,
        *,
        input: InputGrokImagine,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Grok's take on a prompt — punchy, irreverent stills, in everything from ultrawide to tall.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputGrokImagine

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputGrokImagine

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_grok_imagine(
            input=InputGrokImagine(
                prompt="prompt",
            ),
        )
        """
        _response = self._raw_client.submit_grok_imagine(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_grok_imagine20(
        self,
        *,
        input: InputGrokImagine20,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        xAI's current Grok Imagine — the same irreverence at higher fidelity, from a prompt or from up to three source images.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputGrokImagine20

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputGrokImagine20

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_grok_imagine20(
            input=InputGrokImagine20(
                prompt="prompt",
                aspect_ratio="2:1",
                resolution="1k",
            ),
        )
        """
        _response = self._raw_client.submit_grok_imagine20(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_grok_video(
        self,
        *,
        input: InputGrokVideo,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Short, punchy clips from a prompt at 480p or 720p.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputGrokVideo

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputGrokVideo

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_grok_video(
            input=InputGrokVideo(
                prompt="prompt",
                aspect_ratio="auto",
                resolution="480p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_grok_video(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_happy_horse(
        self,
        *,
        input: InputHappyHorse,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Open-weight video generation from a prompt.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputHappyHorse

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputHappyHorse

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_happy_horse(
            input=InputHappyHorse(
                prompt="prompt",
                aspect_ratio="21:9",
                resolution="720p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_happy_horse(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_hedra_avatar(
        self,
        *,
        input: InputHedraAvatar,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Hedra's latest longform avatar model, audio to video will full multi-language support. Perfect for talking and singing video with speaker selection up to 10 minutes long.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputHedraAvatar

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputHedraAvatar,
            InputHedraAvatarAudioZero_Url,
            InputHedraAvatarStartImage_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_hedra_avatar(
            input=InputHedraAvatar(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="540p",
                start_image=InputHedraAvatarStartImage_Url(
                    url="url",
                ),
                audio=InputHedraAvatarAudioZero_Url(
                    url="url",
                ),
            ),
        )
        """
        _response = self._raw_client.submit_hedra_avatar(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_hedra_character3(
        self,
        *,
        input: InputHedraCharacter3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Hedra's latest longform avatar model, audio to video will full multi-language support. Perfect for talking and singing video with speaker selection up to 10 minutes long.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputHedraCharacter3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputHedraCharacter3,
            InputHedraCharacter3AudioZero_Url,
            InputHedraCharacter3StartImage_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_hedra_character3(
            input=InputHedraCharacter3(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="540p",
                start_image=InputHedraCharacter3StartImage_Url(
                    url="url",
                ),
                audio=InputHedraCharacter3AudioZero_Url(
                    url="url",
                ),
            ),
        )
        """
        _response = self._raw_client.submit_hedra_character3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_heygen_photo_avatar4(
        self,
        *,
        input: InputHeygenPhotoAvatar4,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Turn a clear portrait and driving audio into a talking avatar.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputHeygenPhotoAvatar4

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputHeygenPhotoAvatar4,
            InputHeygenPhotoAvatar4Audio_Url,
            InputHeygenPhotoAvatar4StartImage_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_heygen_photo_avatar4(
            input=InputHeygenPhotoAvatar4(
                aspect_ratio="16:9",
                resolution="360p",
                start_image=InputHeygenPhotoAvatar4StartImage_Url(
                    url="url",
                ),
                audio=InputHeygenPhotoAvatar4Audio_Url(
                    url="url",
                ),
            ),
        )
        """
        _response = self._raw_client.submit_heygen_photo_avatar4(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_hidream_o1image(
        self,
        *,
        input: InputHidreamO1Image,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        HiDream.ai's open-weights HiDream-O1-Image (8B): one pixel-native model that generates, edits, and personalizes without a VAE or a separate text encoder.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputHidreamO1Image

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputHidreamO1Image

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_hidream_o1image(
            input=InputHidreamO1Image(
                prompt="prompt",
                aspect_ratio="16:9",
            ),
        )
        """
        _response = self._raw_client.submit_hidream_o1image(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_ideogram_v2(
        self,
        *,
        input: InputIdeogramV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Best in class for poster-ready images and spot-on text rendering in social graphics.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputIdeogramV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputIdeogramV2

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_ideogram_v2(
            input=InputIdeogramV2(
                prompt="prompt",
                aspect_ratio="1:1",
            ),
        )
        """
        _response = self._raw_client.submit_ideogram_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_ideogram_v4(
        self,
        *,
        input: InputIdeogramV4,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Ideogram V4 renders poster-ready text and layout; the required quality parameter picks turbo, balanced or quality, which sets both the render effort and the price.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputIdeogramV4

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputIdeogramV4

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_ideogram_v4(
            input=InputIdeogramV4(
                prompt="prompt",
                aspect_ratio="16:9",
                resolution="720p",
            ),
        )
        """
        _response = self._raw_client.submit_ideogram_v4(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_imagen3(
        self,
        *,
        input: InputImagen3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Google's earlier photoreal generator, kept for parity.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputImagen3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputImagen3

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_imagen3(
            input=InputImagen3(
                prompt="prompt",
                aspect_ratio="16:9",
            ),
        )
        """
        _response = self._raw_client.submit_imagen3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_imagen4(
        self,
        *,
        input: InputImagen4,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Google's photoreal model—natural lighting, lifelike skin, and pro-grade sharpness in every shot.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputImagen4

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputImagen4

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_imagen4(
            input=InputImagen4(
                prompt="prompt",
                aspect_ratio="16:9",
                resolution="1K",
            ),
        )
        """
        _response = self._raw_client.submit_imagen4(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_kling16(
        self,
        *,
        input: InputKling16,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKling16

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputKling16

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_kling16(
            input=InputKling16(
                prompt="prompt",
            ),
        )
        """
        _response = self._raw_client.submit_kling16(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_kling21master(
        self,
        *,
        input: InputKling21Master,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Cinema-grade video with striking textures and rich depth.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKling21Master

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputKling21Master

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_kling21master(
            input=InputKling21Master(
                prompt="prompt",
                aspect_ratio="16:9",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_kling21master(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_kling25turbo(
        self,
        *,
        input: InputKling25Turbo,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Fast, high-quality video generation.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKling25Turbo

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputKling25Turbo

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_kling25turbo(
            input=InputKling25Turbo(
                prompt="prompt",
                aspect_ratio="16:9",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_kling25turbo(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_kling26motion_control(
        self,
        *,
        input: InputKling26MotionControl,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Transfer movements from a reference video to any character image. Cost-effective mode for motion transfer, perfect for portraits and simple animations.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKling26MotionControl

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputKling26MotionControl,
            InputKling26MotionControlSourceVideo_Url,
            InputKling26MotionControlStartImage_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_kling26motion_control(
            input=InputKling26MotionControl(
                start_image=InputKling26MotionControlStartImage_Url(
                    url="url",
                ),
                source_video=InputKling26MotionControlSourceVideo_Url(
                    url="url",
                ),
                resolution="720p",
            ),
        )
        """
        _response = self._raw_client.submit_kling26motion_control(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_kling26pro(
        self,
        *,
        input: InputKling26Pro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Cinematic visuals, fluid motion, and native audio generation.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKling26Pro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputKling26Pro

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_kling26pro(
            input=InputKling26Pro(
                prompt="prompt",
                aspect_ratio="16:9",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_kling26pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_kling_ai_avatar_v2(
        self,
        *,
        input: InputKlingAiAvatarV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Create avatar videos with realistic humans, animals, cartoons, or stylized characters from an image and audio input.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingAiAvatarV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputKlingAiAvatarV2,
            InputKlingAiAvatarV2Audio_Url,
            InputKlingAiAvatarV2StartImage_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_kling_ai_avatar_v2(
            input=InputKlingAiAvatarV2(
                aspect_ratio="16:9",
                start_image=InputKlingAiAvatarV2StartImage_Url(
                    url="url",
                ),
                audio=InputKlingAiAvatarV2Audio_Url(
                    url="url",
                ),
            ),
        )
        """
        _response = self._raw_client.submit_kling_ai_avatar_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_kling_o1(
        self,
        *,
        input: InputKlingO1,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Generate from a single image with text-driven style and scene guidance.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingO1

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputKlingO1

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_kling_o1(
            input=InputKlingO1(
                prompt="prompt",
                aspect_ratio="16:9",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_kling_o1(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_kling_o3(
        self,
        *,
        input: InputKlingO3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Clips up to 15 seconds with native audio.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingO3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputKlingO3

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_kling_o3(
            input=InputKlingO3(
                aspect_ratio="16:9",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_kling_o3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_kling_o3edit(
        self,
        *,
        input: InputKlingO3Edit,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Edit videos using natural language.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingO3Edit

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputKlingO3Edit, InputKlingO3EditSourceVideo_Url

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_kling_o3edit(
            input=InputKlingO3Edit(
                prompt="prompt",
                source_video=InputKlingO3EditSourceVideo_Url(
                    url="url",
                ),
                resolution="720p",
            ),
        )
        """
        _response = self._raw_client.submit_kling_o3edit(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_kling_o3reference(
        self,
        *,
        input: InputKlingO3Reference,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Input a reference video and preserve motion and camera style.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingO3Reference

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputKlingO3Reference,
            InputKlingO3ReferenceSourceVideo_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_kling_o3reference(
            input=InputKlingO3Reference(
                prompt="prompt",
                source_video=InputKlingO3ReferenceSourceVideo_Url(
                    url="url",
                ),
                resolution="720p",
            ),
        )
        """
        _response = self._raw_client.submit_kling_o3reference(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_kling_v3(
        self,
        *,
        input: InputKlingV3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Ultra-high-definition storyboards with native audio.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingV3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputKlingV3

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_kling_v3(
            input=InputKlingV3(
                aspect_ratio="16:9",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_kling_v3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_kling_v3motion_control(
        self,
        *,
        input: InputKlingV3MotionControl,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Animate a character image to match the motion of a reference video. Standard tier for cost-effective generation.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingV3MotionControl

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputKlingV3MotionControl,
            InputKlingV3MotionControlSourceVideo_Url,
            InputKlingV3MotionControlStartImage_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_kling_v3motion_control(
            input=InputKlingV3MotionControl(
                start_image=InputKlingV3MotionControlStartImage_Url(
                    url="url",
                ),
                source_video=InputKlingV3MotionControlSourceVideo_Url(
                    url="url",
                ),
                resolution="720p",
            ),
        )
        """
        _response = self._raw_client.submit_kling_v3motion_control(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_ltx23(
        self,
        *,
        input: InputLtx23,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Clips up to 4K with synchronized native audio, for final output.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputLtx23

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputLtx23

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_ltx23(
            input=InputLtx23(
                prompt="prompt",
                resolution="1080p",
                duration_ms=1,
                aspect_ratio="auto",
            ),
        )
        """
        _response = self._raw_client.submit_ltx23(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_luma_ray32(
        self,
        *,
        input: InputLumaRay32,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Cinematic motion with deliberate camera control, from a prompt.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputLumaRay32

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputLumaRay32

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_luma_ray32(
            input=InputLumaRay32(
                prompt="prompt",
                aspect_ratio="3:4",
                resolution="540p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_luma_ray32(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_mai_image25(
        self,
        *,
        input: InputMaiImage25,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Microsoft AI's MAI-Image-2.5: photorealistic generation and editing with strong in-image typography and design-ready output.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputMaiImage25

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputMaiImage25

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_mai_image25(
            input=InputMaiImage25(
                prompt="prompt",
                aspect_ratio="1:1",
            ),
        )
        """
        _response = self._raw_client.submit_mai_image25(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_minimax_h3(
        self,
        *,
        input: InputMinimaxH3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        One model for every starting point — a prompt, a keyframe pair, or reference images that keep a subject consistent.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputMinimaxH3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputMinimaxH3

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_minimax_h3(
            input=InputMinimaxH3(
                prompt="prompt",
                resolution="480p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_minimax_h3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_minimax_hailuo02(
        self,
        *,
        input: InputMinimaxHailuo02,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Everyday 1080p video with natural movement.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputMinimaxHailuo02

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputMinimaxHailuo02

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_minimax_hailuo02(
            input=InputMinimaxHailuo02(
                prompt="prompt",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_minimax_hailuo02(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_minimax_hailuo23(
        self,
        *,
        input: InputMinimaxHailuo23,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Everyday 1080p video with natural movement.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputMinimaxHailuo23

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputMinimaxHailuo23

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_minimax_hailuo23(
            input=InputMinimaxHailuo23(
                prompt="prompt",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_minimax_hailuo23(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_minimax_speech25hd_preview(
        self,
        *,
        input: InputMinimaxSpeech25HdPreview,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The high-fidelity tier — closest voice likeness, across 40+ languages.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputMinimaxSpeech25HdPreview

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputMinimaxSpeech25HdPreview

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_minimax_speech25hd_preview(
            input=InputMinimaxSpeech25HdPreview(
                text="text",
                voice_id="voice_id",
            ),
        )
        """
        _response = self._raw_client.submit_minimax_speech25hd_preview(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_minimax_speech25turbo_preview(
        self,
        *,
        input: InputMinimaxSpeech25TurboPreview,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The value tier — natural English delivery across 40+ languages, at a lower rate.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputMinimaxSpeech25TurboPreview

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputMinimaxSpeech25TurboPreview

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_minimax_speech25turbo_preview(
            input=InputMinimaxSpeech25TurboPreview(
                text="text",
                voice_id="voice_id",
            ),
        )
        """
        _response = self._raw_client.submit_minimax_speech25turbo_preview(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_nano_banana(
        self,
        *,
        input: InputNanoBanana,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Reference-guided stills that hold a character or product across a set.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputNanoBanana

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputNanoBanana

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_nano_banana(
            input=InputNanoBanana(
                prompt="prompt",
                aspect_ratio="16:9",
                resolution="1K",
            ),
        )
        """
        _response = self._raw_client.submit_nano_banana(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_nano_banana2(
        self,
        *,
        input: InputNanoBanana2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Multi-subject stills up to 4K — hand it several references and it keeps each one recognizable.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputNanoBanana2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputNanoBanana2

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_nano_banana2(
            input=InputNanoBanana2(
                prompt="prompt",
                aspect_ratio="adaptive",
                resolution="512px",
            ),
        )
        """
        _response = self._raw_client.submit_nano_banana2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_nano_banana_pro(
        self,
        *,
        input: InputNanoBananaPro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The reasoning-heavy tier — dense prompts, mixed references, and style transfer up to 4K.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputNanoBananaPro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputNanoBananaPro

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_nano_banana_pro(
            input=InputNanoBananaPro(
                prompt="prompt",
                aspect_ratio="adaptive",
                resolution="1K",
            ),
        )
        """
        _response = self._raw_client.submit_nano_banana_pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_omnihuman15(
        self,
        *,
        input: InputOmnihuman15,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Creates vivid, emotional character videos driven entirely by your audio.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputOmnihuman15

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputOmnihuman15,
            InputOmnihuman15Audio_Url,
            InputOmnihuman15StartImage_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_omnihuman15(
            input=InputOmnihuman15(
                resolution="720p",
                start_image=InputOmnihuman15StartImage_Url(
                    url="url",
                ),
                audio=InputOmnihuman15Audio_Url(
                    url="url",
                ),
            ),
        )
        """
        _response = self._raw_client.submit_omnihuman15(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_pixverse_v6(
        self,
        *,
        input: InputPixverseV6,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Stylized 1080p clips up to 15 seconds, with native audio.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputPixverseV6

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputPixverseV6

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_pixverse_v6(
            input=InputPixverseV6(
                prompt="prompt",
                resolution="360p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_pixverse_v6(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_qwen_image2(
        self,
        *,
        input: InputQwenImage2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Alibaba's Qwen-Image-2.0, tuned for speed. Native 2K output with professional in-image text rendering, for rapid iteration.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputQwenImage2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputQwenImage2

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_qwen_image2(
            input=InputQwenImage2(
                prompt="prompt",
                aspect_ratio="16:9",
                resolution="540p",
            ),
        )
        """
        _response = self._raw_client.submit_qwen_image2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_recraft_v3(
        self,
        *,
        input: InputRecraftV3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Vector-clean graphics and crisp logos on demand.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputRecraftV3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputRecraftV3

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_recraft_v3(
            input=InputRecraftV3(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="540p",
            ),
        )
        """
        _response = self._raw_client.submit_recraft_v3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_reve21(
        self,
        *,
        input: InputReve21,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Generate images from a text prompt with strong prompt adherence, layout intelligence, and accurate text rendering

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputReve21

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputReve21

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_reve21(
            input=InputReve21(
                prompt="prompt",
                aspect_ratio="4:1",
            ),
        )
        """
        _response = self._raw_client.submit_reve21(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_reve21edit(
        self,
        *,
        input: InputReve21Edit,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Edit one source image from a natural-language instruction, keeping the rest of the image intact

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputReve21Edit

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputReve21Edit, InputReve21EditImagesItem_Url

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_reve21edit(
            input=InputReve21Edit(
                prompt="prompt",
                aspect_ratio="4:1",
                images=[
                    InputReve21EditImagesItem_Url(
                        url="url",
                    )
                ],
            ),
        )
        """
        _response = self._raw_client.submit_reve21edit(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_reve21remix(
        self,
        *,
        input: InputReve21Remix,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Compose up to eight reference images into a new image from a text prompt

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputReve21Remix

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputReve21Remix, InputReve21RemixImagesItem_Url

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_reve21remix(
            input=InputReve21Remix(
                prompt="prompt",
                aspect_ratio="4:1",
                images=[
                    InputReve21RemixImagesItem_Url(
                        url="url",
                    )
                ],
            ),
        )
        """
        _response = self._raw_client.submit_reve21remix(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_sana(
        self,
        *,
        input: InputSana,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Lightning-fast and cheap for simple product shots or everyday content.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSana

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputSana

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_sana(
            input=InputSana(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="540p",
            ),
        )
        """
        _response = self._raw_client.submit_sana(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_seedance15pro(
        self,
        *,
        input: InputSeedance15Pro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Keyframe-driven video with native audio, from a start frame, an end frame, or both.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedance15Pro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputSeedance15Pro

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_seedance15pro(
            input=InputSeedance15Pro(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="480p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_seedance15pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_seedance20(
        self,
        *,
        input: InputSeedance20,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Reference-driven video up to 4K with native audio — hold a look across shots with reference images, clips, or audio.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedance20

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputSeedance20

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_seedance20(
            input=InputSeedance20(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="4K",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_seedance20(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_seedance20mini(
        self,
        *,
        input: InputSeedance20Mini,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The lightest Seedance tier — short reference-driven clips at 480p and 720p.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedance20Mini

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputSeedance20Mini

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_seedance20mini(
            input=InputSeedance20Mini(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="480p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_seedance20mini(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_seedance25(
        self,
        *,
        input: InputSeedance25,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Reference-driven video up to 30 seconds at 1080p, with native audio.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedance25

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputSeedance25

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_seedance25(
            input=InputSeedance25(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="480p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_seedance25(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_seedream40(
        self,
        *,
        input: InputSeedream40,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Quick, reference-aware stills for professional work on a tight turnaround.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedream40

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputSeedream40

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_seedream40(
            input=InputSeedream40(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="1080p",
            ),
        )
        """
        _response = self._raw_client.submit_seedream40(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_seedream45(
        self,
        *,
        input: InputSeedream45,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Finer detail and steadier composition than 4.0, with support for several references at once.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedream45

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputSeedream45

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_seedream45(
            input=InputSeedream45(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="1440p (2K QHD)",
            ),
        )
        """
        _response = self._raw_client.submit_seedream45(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_seedream50lite(
        self,
        *,
        input: InputSeedream50Lite,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Sharp 2K and 4K stills from a prompt, at the light tier's price.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedream50Lite

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputSeedream50Lite

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_seedream50lite(
            input=InputSeedream50Lite(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="1440p (2K QHD)",
            ),
        )
        """
        _response = self._raw_client.submit_seedream50lite(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_seedream50pro(
        self,
        *,
        input: InputSeedream50Pro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The top Seedream tier — layer-separable output and strong multilingual in-image text, up to 2K.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedream50Pro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputSeedream50Pro

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_seedream50pro(
            input=InputSeedream50Pro(
                prompt="prompt",
                aspect_ratio="1:1",
                resolution="1080p",
            ),
        )
        """
        _response = self._raw_client.submit_seedream50pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_sora2pro(
        self,
        *,
        input: InputSora2Pro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        For complex, narrative-driven videos with remarkable consistency and realistic character-world interaction.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSora2Pro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputSora2Pro

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_sora2pro(
            input=InputSora2Pro(
                prompt="prompt",
                aspect_ratio="16:9",
                resolution="720p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_sora2pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_topaz_image_upscaler(
        self,
        *,
        input: InputTopazImageUpscaler,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Use the powerful and accurate Topaz image enhancer to upscale and enhance your images.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazImageUpscaler

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputTopazImageUpscaler,
            InputTopazImageUpscalerSourceImage_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_topaz_image_upscaler(
            input=InputTopazImageUpscaler(
                source_image=InputTopazImageUpscalerSourceImage_Url(
                    url="url",
                ),
                target_resolution="1080p",
            ),
        )
        """
        _response = self._raw_client.submit_topaz_image_upscaler(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_topaz_image_upscaler_wonder(
        self,
        *,
        input: InputTopazImageUpscalerWonder,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Generative upscaling with realistic detail, precise text, and clean graphics — Topaz's highest-quality image upscaler.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazImageUpscalerWonder

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputTopazImageUpscalerWonder,
            InputTopazImageUpscalerWonderSourceImage_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_topaz_image_upscaler_wonder(
            input=InputTopazImageUpscalerWonder(
                source_image=InputTopazImageUpscalerWonderSourceImage_Url(
                    url="url",
                ),
                target_resolution="1080p",
            ),
        )
        """
        _response = self._raw_client.submit_topaz_image_upscaler_wonder(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_topaz_video_upscaler(
        self,
        *,
        input: InputTopazVideoUpscaler,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Precision upscaling that cleans compression and noise while staying faithful to the source.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazVideoUpscaler

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputTopazVideoUpscaler,
            InputTopazVideoUpscalerSourceVideo_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_topaz_video_upscaler(
            input=InputTopazVideoUpscaler(
                source_video=InputTopazVideoUpscalerSourceVideo_Url(
                    url="url",
                ),
                resolution="1080p",
            ),
        )
        """
        _response = self._raw_client.submit_topaz_video_upscaler(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_topaz_video_upscaler_hyperion25(
        self,
        *,
        input: InputTopazVideoUpscalerHyperion25,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Convert SDR video to 10-bit HDR with richer highlights, color, and tonal separation. The output keeps the source resolution.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazVideoUpscalerHyperion25

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputTopazVideoUpscalerHyperion25,
            InputTopazVideoUpscalerHyperion25SourceVideo_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_topaz_video_upscaler_hyperion25(
            input=InputTopazVideoUpscalerHyperion25(
                source_video=InputTopazVideoUpscalerHyperion25SourceVideo_Url(
                    url="url",
                ),
            ),
        )
        """
        _response = self._raw_client.submit_topaz_video_upscaler_hyperion25(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_topaz_video_upscaler_starlight_fast(
        self,
        *,
        input: InputTopazVideoUpscalerStarlightFast,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Faster generative diffusion upscaling at half the cost of Starlight Precise.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazVideoUpscalerStarlightFast

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputTopazVideoUpscalerStarlightFast,
            InputTopazVideoUpscalerStarlightFastSourceVideo_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_topaz_video_upscaler_starlight_fast(
            input=InputTopazVideoUpscalerStarlightFast(
                source_video=InputTopazVideoUpscalerStarlightFastSourceVideo_Url(
                    url="url",
                ),
                resolution="1080p",
            ),
        )
        """
        _response = self._raw_client.submit_topaz_video_upscaler_starlight_fast(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_topaz_video_upscaler_starlight_hq(
        self,
        *,
        input: InputTopazVideoUpscalerStarlightHq,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Generative diffusion upscaling balancing detail and sharpness for medium-to-high quality sources.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazVideoUpscalerStarlightHq

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputTopazVideoUpscalerStarlightHq,
            InputTopazVideoUpscalerStarlightHqSourceVideo_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_topaz_video_upscaler_starlight_hq(
            input=InputTopazVideoUpscalerStarlightHq(
                source_video=InputTopazVideoUpscalerStarlightHqSourceVideo_Url(
                    url="url",
                ),
                resolution="1080p",
            ),
        )
        """
        _response = self._raw_client.submit_topaz_video_upscaler_starlight_hq(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_topaz_video_upscaler_starlight_precise(
        self,
        *,
        input: InputTopazVideoUpscalerStarlightPrecise,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Generative diffusion upscaling for AI-generated and archival video with realistic faces, textures, and text.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazVideoUpscalerStarlightPrecise

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputTopazVideoUpscalerStarlightPrecise,
            InputTopazVideoUpscalerStarlightPreciseSourceVideo_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_topaz_video_upscaler_starlight_precise(
            input=InputTopazVideoUpscalerStarlightPrecise(
                source_video=InputTopazVideoUpscalerStarlightPreciseSourceVideo_Url(
                    url="url",
                ),
                resolution="1080p",
            ),
        )
        """
        _response = self._raw_client.submit_topaz_video_upscaler_starlight_precise(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_veed_fabric10(
        self,
        *,
        input: InputVeedFabric10,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Talking video with natural lip-sync and expressive animation.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputVeedFabric10

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputVeedFabric10,
            InputVeedFabric10Audio_Url,
            InputVeedFabric10StartImage_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_veed_fabric10(
            input=InputVeedFabric10(
                prompt="prompt",
                aspect_ratio="16:9",
                resolution="480p",
                start_image=InputVeedFabric10StartImage_Url(
                    url="url",
                ),
                audio=InputVeedFabric10Audio_Url(
                    url="url",
                ),
            ),
        )
        """
        _response = self._raw_client.submit_veed_fabric10(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_veed_video_background_removal(
        self,
        *,
        input: InputVeedVideoBackgroundRemoval,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Remove a video's background and return transparent WebM.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputVeedVideoBackgroundRemoval

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputVeedVideoBackgroundRemoval,
            InputVeedVideoBackgroundRemovalSourceVideo_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_veed_video_background_removal(
            input=InputVeedVideoBackgroundRemoval(
                source_video=InputVeedVideoBackgroundRemovalSourceVideo_Url(
                    url="url",
                ),
            ),
        )
        """
        _response = self._raw_client.submit_veed_video_background_removal(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_veo2(
        self,
        *,
        input: InputVeo2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Google's earlier cinematic generator, kept for existing workflows.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputVeo2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputVeo2

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_veo2(
            input=InputVeo2(
                prompt="prompt",
            ),
        )
        """
        _response = self._raw_client.submit_veo2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_veo3(
        self,
        *,
        input: InputVeo3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Hollywood-grade, cinematic video straight from text—your go-to for hero campaigns.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputVeo3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputVeo3

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_veo3(
            input=InputVeo3(
                prompt="prompt",
                aspect_ratio="16:9",
                resolution="720p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_veo3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_veo31(
        self,
        *,
        input: InputVeo31,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        For unparalleled detail and nuance, perfect for when your vision requires the best possible quality.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputVeo31

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputVeo31

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_veo31(
            input=InputVeo31(
                prompt="prompt",
                aspect_ratio="16:9",
                resolution="720p",
            ),
        )
        """
        _response = self._raw_client.submit_veo31(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_vidu_q3(
        self,
        *,
        input: InputViduQ3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The longest clips in the catalog — up to 16 seconds with native dialogue and sound, from a text prompt, from a start frame, or between a start and end frame

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputViduQ3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputViduQ3

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_vidu_q3(
            input=InputViduQ3(
                prompt="prompt",
                resolution="540p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_vidu_q3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_vidu_q3reference(
        self,
        *,
        input: InputViduQ3Reference,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Keep up to four subjects consistent across a clip from reference images.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputViduQ3Reference

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import (
            Hedra,
            InputViduQ3Reference,
            InputViduQ3ReferenceImagesItem_Url,
        )

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_vidu_q3reference(
            input=InputViduQ3Reference(
                prompt="prompt",
                aspect_ratio="16:9",
                resolution="540p",
                duration_ms=1,
                images=[
                    InputViduQ3ReferenceImagesItem_Url(
                        url="url",
                    )
                ],
            ),
        )
        """
        _response = self._raw_client.submit_vidu_q3reference(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_wan27(
        self,
        *,
        input: InputWan27,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Wan 2.7 video with native audio — from a text prompt, from a first frame with an optional last frame, or from reference images that keep subjects consistent

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputWan27

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputWan27

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_wan27(
            input=InputWan27(
                prompt="prompt",
                resolution="720p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_wan27(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit_wan30(
        self,
        *,
        input: InputWan30,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Wan 3.0 video with native audio, up to 30 seconds in one shot — from a text prompt, from a first frame with an optional last frame, or from reference images that keep subjects consistent

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputWan30

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra, InputWan30

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit_wan30(
            input=InputWan30(
                prompt="prompt",
                aspect_ratio="adaptive",
                resolution="480p",
                duration_ms=1,
            ),
        )
        """
        _response = self._raw_client.submit_wan30(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def submit(
        self,
        model: str,
        *,
        input: typing.Dict[str, typing.Any],
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Runs any model in the catalog by its public id, with `input` passed through untyped — the same call the typed operations below make, minus the compile-time schema.

        Reach for it when the model is not known ahead of time: a client generated before a model shipped can still run it, and an id read from `GET /v3/models` at runtime needs no regeneration. Prefer the typed operation whenever your client already has one — `input` here is validated against the same published schema (`GET /v3/models/{model}`), so a bad field is a `400` at submit rather than an error before the call.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        model : str
            The model's public id (`GET /v3/models`).

        input : typing.Dict[str, typing.Any]
            Model-specific inputs, validated at submit against the model's published input schema (`GET /v3/models/{model}`).

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        from hedra import Hedra

        client = Hedra(
            api_key="YOUR_API_KEY",
        )
        client.jobs.submit(
            model="model",
            input={"key": "value"},
        )
        """
        _response = self._raw_client.submit(
            model, input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data


class AsyncJobsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawJobsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawJobsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawJobsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[JobSummary, JobListResponse]:
        """
        Parameters
        ----------
        limit : typing.Optional[int]
            Maximum items per page.

        cursor : typing.Optional[str]
            Opaque cursor from the previous page's `next_cursor`; omit for the first page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[JobSummary, JobListResponse]
            Successful Response

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = await client.jobs.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(limit=limit, cursor=cursor, request_options=request_options)

    async def get(self, job_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> ResultResponse:
        """
        Parameters
        ----------
        job_id : str
            The job's id (`job_<uuid>`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResultResponse
            Successful Response

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.get(
                job_id="job_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(job_id, request_options=request_options)
        return _response.data

    async def get_status(
        self,
        job_id: str,
        *,
        logs_after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StatusResponse:
        """
        Parameters
        ----------
        job_id : str
            The job's id (`job_<uuid>`).

        logs_after : typing.Optional[str]
            Tail this job's lifecycle events incrementally: returns only events newer than this cursor, plus `logs_next_cursor` to send on the next poll. Pass `start` to begin from the job's first event. Omit it and the response carries no events at all — the default polling shape is unchanged.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StatusResponse
            Successful Response

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.get_status(
                job_id="job_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_status(job_id, logs_after=logs_after, request_options=request_options)
        return _response.data

    async def list_job_logs(
        self,
        job_id: str,
        *,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[JobLogItem, JobLogListResponse]:
        """
        Parameters
        ----------
        job_id : str
            The job's id (`job_<uuid>`).

        limit : typing.Optional[int]
            Maximum items per page.

        cursor : typing.Optional[str]
            Opaque cursor from the previous page's `next_cursor`; omit for the first page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[JobLogItem, JobLogListResponse]
            Successful Response

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = await client.jobs.list_job_logs(
                job_id="job_id",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list_job_logs(job_id, limit=limit, cursor=cursor, request_options=request_options)

    async def stream(
        self,
        job_id: str,
        *,
        last_event_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.AsyncIterator[JobsStreamResponse]:
        """
        Parameters
        ----------
        job_id : str
            The job's id (`job_<uuid>`).

        last_event_id : typing.Optional[str]
            Resume after this event id — the standard SSE reconnect header; browsers' EventSource sends it automatically.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Yields
        ------
        typing.AsyncIterator[JobsStreamResponse]
            Server-Sent Events. Each frame's `event:` names its type. The `data:` line of a `status` frame is a `StatusResponse` and of a `log` frame a `JobLogItem` (the same row `GET /jobs/{job_id}/logs` serves), both JSON. The stream opens with a `status` snapshot and ends with a `done` frame, sent after the terminal `status` frame: reaching it means the job finished (`COMPLETED` or `FAILED`) and there is nothing more to read. A stream that ends without it was cut short; reconnect with `Last-Event-Id`. Closes the server initiates instead (drain, max connection duration) send a `retry:` line and no `done`. **The `done` frame's `data:` line is the literal token `[STREAM_DONE]` — not JSON, and not a member of the `JobStreamFrame` schema below.** It is a fixed end-of-stream sentinel, matched verbatim; switch on `event:` and skip `done` before parsing a `data:` line, or the parse fails on the last frame of every stream. It repeats no state: which job ended and how was just delivered by the terminal `status` frame. `status` and `log` frames carry an `id:` to send back as `Last-Event-Id`; `done` does not, since it is not a new stream position. Comment lines (`: keep-alive`) and `retry:` lines are protocol-level and carry no payload.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = client.jobs.stream(
                job_id="job_id",
            )
            async for chunk in response:
                yield chunk


        asyncio.run(main())
        """
        async with self._raw_client.stream(job_id, last_event_id=last_event_id, request_options=request_options) as r:
            async for _chunk in r.data:
                yield _chunk

    async def submit_creatify_aurora(
        self,
        *,
        input: InputCreatifyAurora,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Create high-fidelity speaking or singing avatar videos.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputCreatifyAurora

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputCreatifyAurora,
            InputCreatifyAuroraAudio_Url,
            InputCreatifyAuroraStartImage_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_creatify_aurora(
                input=InputCreatifyAurora(
                    resolution="480p",
                    start_image=InputCreatifyAuroraStartImage_Url(
                        url="url",
                    ),
                    audio=InputCreatifyAuroraAudio_Url(
                        url="url",
                    ),
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_creatify_aurora(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_dreamina31(
        self,
        *,
        input: InputDreamina31,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Polished, print-ready stills when the brief is a finished image rather than a sketch.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputDreamina31

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputDreamina31

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_dreamina31(
                input=InputDreamina31(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    resolution="540p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_dreamina31(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_elevenlabs_audio_isolation(
        self,
        *,
        input: InputElevenlabsAudioIsolation,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Strip background noise from a recording, keeping the speech.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsAudioIsolation

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputElevenlabsAudioIsolation,
            InputElevenlabsAudioIsolationAudio_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_elevenlabs_audio_isolation(
                input=InputElevenlabsAudioIsolation(
                    audio=InputElevenlabsAudioIsolationAudio_Url(
                        url="url",
                    ),
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_elevenlabs_audio_isolation(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_elevenlabs_english_sts_v2(
        self,
        *,
        input: InputElevenlabsEnglishStsV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsEnglishStsV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputElevenlabsEnglishStsV2,
            InputElevenlabsEnglishStsV2Audio_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_elevenlabs_english_sts_v2(
                input=InputElevenlabsEnglishStsV2(
                    audio=InputElevenlabsEnglishStsV2Audio_Url(
                        url="url",
                    ),
                    voice_id="voice_id",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_elevenlabs_english_sts_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_elevenlabs_flash_multilingual_v2(
        self,
        *,
        input: InputElevenlabsFlashMultilingualV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The low-latency voice across 30+ languages, for interactive and high-volume speech.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsFlashMultilingualV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputElevenlabsFlashMultilingualV2

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_elevenlabs_flash_multilingual_v2(
                input=InputElevenlabsFlashMultilingualV2(
                    text="text",
                    voice_id="voice_id",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_elevenlabs_flash_multilingual_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_elevenlabs_flash_v2(
        self,
        *,
        input: InputElevenlabsFlashV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The low-latency English voice, for interactive speech.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsFlashV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputElevenlabsFlashV2

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_elevenlabs_flash_v2(
                input=InputElevenlabsFlashV2(
                    text="text",
                    voice_id="voice_id",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_elevenlabs_flash_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_elevenlabs_multilingual_sts_v2(
        self,
        *,
        input: InputElevenlabsMultilingualStsV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsMultilingualStsV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputElevenlabsMultilingualStsV2,
            InputElevenlabsMultilingualStsV2Audio_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_elevenlabs_multilingual_sts_v2(
                input=InputElevenlabsMultilingualStsV2(
                    audio=InputElevenlabsMultilingualStsV2Audio_Url(
                        url="url",
                    ),
                    voice_id="voice_id",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_elevenlabs_multilingual_sts_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_elevenlabs_multilingual_v2(
        self,
        *,
        input: InputElevenlabsMultilingualV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Steady, natural narration across 30+ languages, for finished voiceover.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsMultilingualV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputElevenlabsMultilingualV2

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_elevenlabs_multilingual_v2(
                input=InputElevenlabsMultilingualV2(
                    text="text",
                    voice_id="voice_id",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_elevenlabs_multilingual_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_elevenlabs_music(
        self,
        *,
        input: InputElevenlabsMusic,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Full tracks from a written brief, with optional lyrics placed across the length you ask for.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsMusic

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputElevenlabsMusic

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_elevenlabs_music(
                input=InputElevenlabsMusic(
                    prompt="prompt",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_elevenlabs_music(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_elevenlabs_sound_effects(
        self,
        *,
        input: InputElevenlabsSoundEffects,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        One-off sound effects from a written description, loopable on request.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsSoundEffects

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputElevenlabsSoundEffects

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_elevenlabs_sound_effects(
                input=InputElevenlabsSoundEffects(
                    text="text",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_elevenlabs_sound_effects(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_elevenlabs_v3(
        self,
        *,
        input: InputElevenlabsV3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The most expressive ElevenLabs voice — emotional range and delivery cues for performance, not just narration.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsV3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputElevenlabsV3

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_elevenlabs_v3(
                input=InputElevenlabsV3(
                    text="text",
                    voice_id="voice_id",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_elevenlabs_v3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_elevenlabs_voice_clone(
        self,
        *,
        input: InputElevenlabsVoiceClone,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Use an audio clip to create a new Voice.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputElevenlabsVoiceClone

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputElevenlabsVoiceClone,
            InputElevenlabsVoiceCloneAudio_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_elevenlabs_voice_clone(
                input=InputElevenlabsVoiceClone(
                    audio=InputElevenlabsVoiceCloneAudio_Url(
                        url="url",
                    ),
                    name="name",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_elevenlabs_voice_clone(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_flux11pro(
        self,
        *,
        input: InputFlux11Pro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Premium color depth and clarity when you want campaign-ready art that feels handcrafted.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux11Pro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputFlux11Pro

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_flux11pro(
                input=InputFlux11Pro(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="540p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_flux11pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_flux11ultra(
        self,
        *,
        input: InputFlux11Ultra,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The big-canvas choice for ultra-high-res images and flagship visuals.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux11Ultra

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputFlux11Ultra

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_flux11ultra(
                input=InputFlux11Ultra(
                    prompt="prompt",
                    aspect_ratio="16:9",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_flux11ultra(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_flux3(
        self,
        *,
        input: InputFlux3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Video with native audio, straight from a prompt.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputFlux3

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_flux3(
                input=InputFlux3(
                    prompt="prompt",
                    aspect_ratio="auto",
                    resolution="720p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_flux3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_flux_dev(
        self,
        *,
        input: InputFluxDev,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Fast and light for quick concepts or high-volume social posts on a budget.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFluxDev

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputFluxDev

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_flux_dev(
                input=InputFluxDev(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="540p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_flux_dev(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_flux_kontext_max(
        self,
        *,
        input: InputFluxKontextMax,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Highest-fidelity reference-image support for complex, multi-element scenes and perfectly matched branded visuals.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFluxKontextMax

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputFluxKontextMax

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_flux_kontext_max(
                input=InputFluxKontextMax(
                    prompt="prompt",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_flux_kontext_max(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_flux_kontext_pro(
        self,
        *,
        input: InputFluxKontextPro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Reference-image support for character, brand, or style consistency.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFluxKontextPro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputFluxKontextPro

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_flux_kontext_pro(
                input=InputFluxKontextPro(
                    prompt="prompt",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_flux_kontext_pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_flux2flex(
        self,
        *,
        input: InputFlux2Flex,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The tunable Flux.2 tier — trade denoising steps against speed per generation.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux2Flex

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputFlux2Flex

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_flux2flex(
                input=InputFlux2Flex(
                    prompt="prompt",
                    aspect_ratio="16:9",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_flux2flex(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_flux2klein9b(
        self,
        *,
        input: InputFlux2Klein9B,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The lean Flux.2 tier — quick, inexpensive stills for concepting and high-volume work.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux2Klein9B

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputFlux2Klein9B

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_flux2klein9b(
                input=InputFlux2Klein9B(
                    prompt="prompt",
                    aspect_ratio="16:9",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_flux2klein9b(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_flux2max(
        self,
        *,
        input: InputFlux2Max,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The top Flux.2 tier, for realism and precision in final deliverables.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux2Max

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputFlux2Max

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_flux2max(
                input=InputFlux2Max(
                    prompt="prompt",
                    aspect_ratio="16:9",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_flux2max(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_flux2pro(
        self,
        *,
        input: InputFlux2Pro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The everyday Flux.2 tier — style transfer and sequential edits that hold together across passes.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputFlux2Pro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputFlux2Pro

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_flux2pro(
                input=InputFlux2Pro(
                    prompt="prompt",
                    aspect_ratio="16:9",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_flux2pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_gemini_omni_flash(
        self,
        *,
        input: InputGeminiOmniFlash,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Gemini's fast multimodal video model — cinematic clips with native audio from a prompt, a keyframe, or reference images.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputGeminiOmniFlash

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputGeminiOmniFlash

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_gemini_omni_flash(
                input=InputGeminiOmniFlash(
                    prompt="prompt",
                    aspect_ratio="16:9",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_gemini_omni_flash(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_gpt_image15(
        self,
        *,
        input: InputGptImage15,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Reads a long, specific brief closely — the choice when the prompt carries the detail.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputGptImage15

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputGptImage15

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_gpt_image15(
                input=InputGptImage15(
                    prompt="prompt",
                    aspect_ratio="1:1",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_gpt_image15(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_gpt_image2(
        self,
        *,
        input: InputGptImage2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        OpenAI's balanced tier; moderate cost and fidelity, ideal for iterative refinement and everyday generation.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputGptImage2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputGptImage2

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_gpt_image2(
                input=InputGptImage2(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    resolution="1K",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_gpt_image2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_grok_imagine(
        self,
        *,
        input: InputGrokImagine,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Grok's take on a prompt — punchy, irreverent stills, in everything from ultrawide to tall.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputGrokImagine

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputGrokImagine

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_grok_imagine(
                input=InputGrokImagine(
                    prompt="prompt",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_grok_imagine(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_grok_imagine20(
        self,
        *,
        input: InputGrokImagine20,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        xAI's current Grok Imagine — the same irreverence at higher fidelity, from a prompt or from up to three source images.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputGrokImagine20

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputGrokImagine20

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_grok_imagine20(
                input=InputGrokImagine20(
                    prompt="prompt",
                    aspect_ratio="2:1",
                    resolution="1k",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_grok_imagine20(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_grok_video(
        self,
        *,
        input: InputGrokVideo,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Short, punchy clips from a prompt at 480p or 720p.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputGrokVideo

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputGrokVideo

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_grok_video(
                input=InputGrokVideo(
                    prompt="prompt",
                    aspect_ratio="auto",
                    resolution="480p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_grok_video(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_happy_horse(
        self,
        *,
        input: InputHappyHorse,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Open-weight video generation from a prompt.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputHappyHorse

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputHappyHorse

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_happy_horse(
                input=InputHappyHorse(
                    prompt="prompt",
                    aspect_ratio="21:9",
                    resolution="720p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_happy_horse(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_hedra_avatar(
        self,
        *,
        input: InputHedraAvatar,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Hedra's latest longform avatar model, audio to video will full multi-language support. Perfect for talking and singing video with speaker selection up to 10 minutes long.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputHedraAvatar

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputHedraAvatar,
            InputHedraAvatarAudioZero_Url,
            InputHedraAvatarStartImage_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_hedra_avatar(
                input=InputHedraAvatar(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="540p",
                    start_image=InputHedraAvatarStartImage_Url(
                        url="url",
                    ),
                    audio=InputHedraAvatarAudioZero_Url(
                        url="url",
                    ),
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_hedra_avatar(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_hedra_character3(
        self,
        *,
        input: InputHedraCharacter3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Hedra's latest longform avatar model, audio to video will full multi-language support. Perfect for talking and singing video with speaker selection up to 10 minutes long.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputHedraCharacter3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputHedraCharacter3,
            InputHedraCharacter3AudioZero_Url,
            InputHedraCharacter3StartImage_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_hedra_character3(
                input=InputHedraCharacter3(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="540p",
                    start_image=InputHedraCharacter3StartImage_Url(
                        url="url",
                    ),
                    audio=InputHedraCharacter3AudioZero_Url(
                        url="url",
                    ),
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_hedra_character3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_heygen_photo_avatar4(
        self,
        *,
        input: InputHeygenPhotoAvatar4,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Turn a clear portrait and driving audio into a talking avatar.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputHeygenPhotoAvatar4

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputHeygenPhotoAvatar4,
            InputHeygenPhotoAvatar4Audio_Url,
            InputHeygenPhotoAvatar4StartImage_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_heygen_photo_avatar4(
                input=InputHeygenPhotoAvatar4(
                    aspect_ratio="16:9",
                    resolution="360p",
                    start_image=InputHeygenPhotoAvatar4StartImage_Url(
                        url="url",
                    ),
                    audio=InputHeygenPhotoAvatar4Audio_Url(
                        url="url",
                    ),
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_heygen_photo_avatar4(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_hidream_o1image(
        self,
        *,
        input: InputHidreamO1Image,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        HiDream.ai's open-weights HiDream-O1-Image (8B): one pixel-native model that generates, edits, and personalizes without a VAE or a separate text encoder.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputHidreamO1Image

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputHidreamO1Image

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_hidream_o1image(
                input=InputHidreamO1Image(
                    prompt="prompt",
                    aspect_ratio="16:9",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_hidream_o1image(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_ideogram_v2(
        self,
        *,
        input: InputIdeogramV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Best in class for poster-ready images and spot-on text rendering in social graphics.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputIdeogramV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputIdeogramV2

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_ideogram_v2(
                input=InputIdeogramV2(
                    prompt="prompt",
                    aspect_ratio="1:1",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_ideogram_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_ideogram_v4(
        self,
        *,
        input: InputIdeogramV4,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Ideogram V4 renders poster-ready text and layout; the required quality parameter picks turbo, balanced or quality, which sets both the render effort and the price.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputIdeogramV4

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputIdeogramV4

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_ideogram_v4(
                input=InputIdeogramV4(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    resolution="720p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_ideogram_v4(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_imagen3(
        self,
        *,
        input: InputImagen3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Google's earlier photoreal generator, kept for parity.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputImagen3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputImagen3

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_imagen3(
                input=InputImagen3(
                    prompt="prompt",
                    aspect_ratio="16:9",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_imagen3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_imagen4(
        self,
        *,
        input: InputImagen4,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Google's photoreal model—natural lighting, lifelike skin, and pro-grade sharpness in every shot.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputImagen4

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputImagen4

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_imagen4(
                input=InputImagen4(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    resolution="1K",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_imagen4(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_kling16(
        self,
        *,
        input: InputKling16,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKling16

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputKling16

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_kling16(
                input=InputKling16(
                    prompt="prompt",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_kling16(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_kling21master(
        self,
        *,
        input: InputKling21Master,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Cinema-grade video with striking textures and rich depth.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKling21Master

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputKling21Master

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_kling21master(
                input=InputKling21Master(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_kling21master(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_kling25turbo(
        self,
        *,
        input: InputKling25Turbo,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Fast, high-quality video generation.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKling25Turbo

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputKling25Turbo

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_kling25turbo(
                input=InputKling25Turbo(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_kling25turbo(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_kling26motion_control(
        self,
        *,
        input: InputKling26MotionControl,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Transfer movements from a reference video to any character image. Cost-effective mode for motion transfer, perfect for portraits and simple animations.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKling26MotionControl

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputKling26MotionControl,
            InputKling26MotionControlSourceVideo_Url,
            InputKling26MotionControlStartImage_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_kling26motion_control(
                input=InputKling26MotionControl(
                    start_image=InputKling26MotionControlStartImage_Url(
                        url="url",
                    ),
                    source_video=InputKling26MotionControlSourceVideo_Url(
                        url="url",
                    ),
                    resolution="720p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_kling26motion_control(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_kling26pro(
        self,
        *,
        input: InputKling26Pro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Cinematic visuals, fluid motion, and native audio generation.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKling26Pro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputKling26Pro

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_kling26pro(
                input=InputKling26Pro(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_kling26pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_kling_ai_avatar_v2(
        self,
        *,
        input: InputKlingAiAvatarV2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Create avatar videos with realistic humans, animals, cartoons, or stylized characters from an image and audio input.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingAiAvatarV2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputKlingAiAvatarV2,
            InputKlingAiAvatarV2Audio_Url,
            InputKlingAiAvatarV2StartImage_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_kling_ai_avatar_v2(
                input=InputKlingAiAvatarV2(
                    aspect_ratio="16:9",
                    start_image=InputKlingAiAvatarV2StartImage_Url(
                        url="url",
                    ),
                    audio=InputKlingAiAvatarV2Audio_Url(
                        url="url",
                    ),
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_kling_ai_avatar_v2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_kling_o1(
        self,
        *,
        input: InputKlingO1,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Generate from a single image with text-driven style and scene guidance.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingO1

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputKlingO1

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_kling_o1(
                input=InputKlingO1(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_kling_o1(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_kling_o3(
        self,
        *,
        input: InputKlingO3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Clips up to 15 seconds with native audio.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingO3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputKlingO3

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_kling_o3(
                input=InputKlingO3(
                    aspect_ratio="16:9",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_kling_o3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_kling_o3edit(
        self,
        *,
        input: InputKlingO3Edit,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Edit videos using natural language.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingO3Edit

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputKlingO3Edit, InputKlingO3EditSourceVideo_Url

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_kling_o3edit(
                input=InputKlingO3Edit(
                    prompt="prompt",
                    source_video=InputKlingO3EditSourceVideo_Url(
                        url="url",
                    ),
                    resolution="720p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_kling_o3edit(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_kling_o3reference(
        self,
        *,
        input: InputKlingO3Reference,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Input a reference video and preserve motion and camera style.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingO3Reference

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputKlingO3Reference,
            InputKlingO3ReferenceSourceVideo_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_kling_o3reference(
                input=InputKlingO3Reference(
                    prompt="prompt",
                    source_video=InputKlingO3ReferenceSourceVideo_Url(
                        url="url",
                    ),
                    resolution="720p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_kling_o3reference(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_kling_v3(
        self,
        *,
        input: InputKlingV3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Ultra-high-definition storyboards with native audio.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingV3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputKlingV3

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_kling_v3(
                input=InputKlingV3(
                    aspect_ratio="16:9",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_kling_v3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_kling_v3motion_control(
        self,
        *,
        input: InputKlingV3MotionControl,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Animate a character image to match the motion of a reference video. Standard tier for cost-effective generation.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputKlingV3MotionControl

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputKlingV3MotionControl,
            InputKlingV3MotionControlSourceVideo_Url,
            InputKlingV3MotionControlStartImage_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_kling_v3motion_control(
                input=InputKlingV3MotionControl(
                    start_image=InputKlingV3MotionControlStartImage_Url(
                        url="url",
                    ),
                    source_video=InputKlingV3MotionControlSourceVideo_Url(
                        url="url",
                    ),
                    resolution="720p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_kling_v3motion_control(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_ltx23(
        self,
        *,
        input: InputLtx23,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Clips up to 4K with synchronized native audio, for final output.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputLtx23

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputLtx23

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_ltx23(
                input=InputLtx23(
                    prompt="prompt",
                    resolution="1080p",
                    duration_ms=1,
                    aspect_ratio="auto",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_ltx23(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_luma_ray32(
        self,
        *,
        input: InputLumaRay32,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Cinematic motion with deliberate camera control, from a prompt.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputLumaRay32

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputLumaRay32

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_luma_ray32(
                input=InputLumaRay32(
                    prompt="prompt",
                    aspect_ratio="3:4",
                    resolution="540p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_luma_ray32(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_mai_image25(
        self,
        *,
        input: InputMaiImage25,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Microsoft AI's MAI-Image-2.5: photorealistic generation and editing with strong in-image typography and design-ready output.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputMaiImage25

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputMaiImage25

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_mai_image25(
                input=InputMaiImage25(
                    prompt="prompt",
                    aspect_ratio="1:1",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_mai_image25(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_minimax_h3(
        self,
        *,
        input: InputMinimaxH3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        One model for every starting point — a prompt, a keyframe pair, or reference images that keep a subject consistent.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputMinimaxH3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputMinimaxH3

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_minimax_h3(
                input=InputMinimaxH3(
                    prompt="prompt",
                    resolution="480p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_minimax_h3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_minimax_hailuo02(
        self,
        *,
        input: InputMinimaxHailuo02,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Everyday 1080p video with natural movement.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputMinimaxHailuo02

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputMinimaxHailuo02

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_minimax_hailuo02(
                input=InputMinimaxHailuo02(
                    prompt="prompt",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_minimax_hailuo02(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_minimax_hailuo23(
        self,
        *,
        input: InputMinimaxHailuo23,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Everyday 1080p video with natural movement.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputMinimaxHailuo23

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputMinimaxHailuo23

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_minimax_hailuo23(
                input=InputMinimaxHailuo23(
                    prompt="prompt",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_minimax_hailuo23(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_minimax_speech25hd_preview(
        self,
        *,
        input: InputMinimaxSpeech25HdPreview,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The high-fidelity tier — closest voice likeness, across 40+ languages.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputMinimaxSpeech25HdPreview

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputMinimaxSpeech25HdPreview

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_minimax_speech25hd_preview(
                input=InputMinimaxSpeech25HdPreview(
                    text="text",
                    voice_id="voice_id",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_minimax_speech25hd_preview(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_minimax_speech25turbo_preview(
        self,
        *,
        input: InputMinimaxSpeech25TurboPreview,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The value tier — natural English delivery across 40+ languages, at a lower rate.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputMinimaxSpeech25TurboPreview

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputMinimaxSpeech25TurboPreview

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_minimax_speech25turbo_preview(
                input=InputMinimaxSpeech25TurboPreview(
                    text="text",
                    voice_id="voice_id",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_minimax_speech25turbo_preview(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_nano_banana(
        self,
        *,
        input: InputNanoBanana,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Reference-guided stills that hold a character or product across a set.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputNanoBanana

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputNanoBanana

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_nano_banana(
                input=InputNanoBanana(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    resolution="1K",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_nano_banana(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_nano_banana2(
        self,
        *,
        input: InputNanoBanana2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Multi-subject stills up to 4K — hand it several references and it keeps each one recognizable.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputNanoBanana2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputNanoBanana2

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_nano_banana2(
                input=InputNanoBanana2(
                    prompt="prompt",
                    aspect_ratio="adaptive",
                    resolution="512px",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_nano_banana2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_nano_banana_pro(
        self,
        *,
        input: InputNanoBananaPro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The reasoning-heavy tier — dense prompts, mixed references, and style transfer up to 4K.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputNanoBananaPro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputNanoBananaPro

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_nano_banana_pro(
                input=InputNanoBananaPro(
                    prompt="prompt",
                    aspect_ratio="adaptive",
                    resolution="1K",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_nano_banana_pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_omnihuman15(
        self,
        *,
        input: InputOmnihuman15,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Creates vivid, emotional character videos driven entirely by your audio.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputOmnihuman15

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputOmnihuman15,
            InputOmnihuman15Audio_Url,
            InputOmnihuman15StartImage_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_omnihuman15(
                input=InputOmnihuman15(
                    resolution="720p",
                    start_image=InputOmnihuman15StartImage_Url(
                        url="url",
                    ),
                    audio=InputOmnihuman15Audio_Url(
                        url="url",
                    ),
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_omnihuman15(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_pixverse_v6(
        self,
        *,
        input: InputPixverseV6,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Stylized 1080p clips up to 15 seconds, with native audio.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputPixverseV6

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputPixverseV6

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_pixverse_v6(
                input=InputPixverseV6(
                    prompt="prompt",
                    resolution="360p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_pixverse_v6(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_qwen_image2(
        self,
        *,
        input: InputQwenImage2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Alibaba's Qwen-Image-2.0, tuned for speed. Native 2K output with professional in-image text rendering, for rapid iteration.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputQwenImage2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputQwenImage2

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_qwen_image2(
                input=InputQwenImage2(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    resolution="540p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_qwen_image2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_recraft_v3(
        self,
        *,
        input: InputRecraftV3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Vector-clean graphics and crisp logos on demand.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputRecraftV3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputRecraftV3

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_recraft_v3(
                input=InputRecraftV3(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="540p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_recraft_v3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_reve21(
        self,
        *,
        input: InputReve21,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Generate images from a text prompt with strong prompt adherence, layout intelligence, and accurate text rendering

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputReve21

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputReve21

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_reve21(
                input=InputReve21(
                    prompt="prompt",
                    aspect_ratio="4:1",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_reve21(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_reve21edit(
        self,
        *,
        input: InputReve21Edit,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Edit one source image from a natural-language instruction, keeping the rest of the image intact

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputReve21Edit

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputReve21Edit, InputReve21EditImagesItem_Url

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_reve21edit(
                input=InputReve21Edit(
                    prompt="prompt",
                    aspect_ratio="4:1",
                    images=[
                        InputReve21EditImagesItem_Url(
                            url="url",
                        )
                    ],
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_reve21edit(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_reve21remix(
        self,
        *,
        input: InputReve21Remix,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Compose up to eight reference images into a new image from a text prompt

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputReve21Remix

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputReve21Remix, InputReve21RemixImagesItem_Url

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_reve21remix(
                input=InputReve21Remix(
                    prompt="prompt",
                    aspect_ratio="4:1",
                    images=[
                        InputReve21RemixImagesItem_Url(
                            url="url",
                        )
                    ],
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_reve21remix(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_sana(
        self,
        *,
        input: InputSana,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Lightning-fast and cheap for simple product shots or everyday content.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSana

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputSana

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_sana(
                input=InputSana(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="540p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_sana(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_seedance15pro(
        self,
        *,
        input: InputSeedance15Pro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Keyframe-driven video with native audio, from a start frame, an end frame, or both.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedance15Pro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputSeedance15Pro

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_seedance15pro(
                input=InputSeedance15Pro(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="480p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_seedance15pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_seedance20(
        self,
        *,
        input: InputSeedance20,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Reference-driven video up to 4K with native audio — hold a look across shots with reference images, clips, or audio.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedance20

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputSeedance20

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_seedance20(
                input=InputSeedance20(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="4K",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_seedance20(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_seedance20mini(
        self,
        *,
        input: InputSeedance20Mini,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The lightest Seedance tier — short reference-driven clips at 480p and 720p.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedance20Mini

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputSeedance20Mini

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_seedance20mini(
                input=InputSeedance20Mini(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="480p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_seedance20mini(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_seedance25(
        self,
        *,
        input: InputSeedance25,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Reference-driven video up to 30 seconds at 1080p, with native audio.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedance25

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputSeedance25

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_seedance25(
                input=InputSeedance25(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="480p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_seedance25(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_seedream40(
        self,
        *,
        input: InputSeedream40,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Quick, reference-aware stills for professional work on a tight turnaround.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedream40

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputSeedream40

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_seedream40(
                input=InputSeedream40(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="1080p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_seedream40(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_seedream45(
        self,
        *,
        input: InputSeedream45,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Finer detail and steadier composition than 4.0, with support for several references at once.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedream45

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputSeedream45

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_seedream45(
                input=InputSeedream45(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="1440p (2K QHD)",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_seedream45(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_seedream50lite(
        self,
        *,
        input: InputSeedream50Lite,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Sharp 2K and 4K stills from a prompt, at the light tier's price.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedream50Lite

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputSeedream50Lite

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_seedream50lite(
                input=InputSeedream50Lite(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="1440p (2K QHD)",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_seedream50lite(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_seedream50pro(
        self,
        *,
        input: InputSeedream50Pro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The top Seedream tier — layer-separable output and strong multilingual in-image text, up to 2K.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSeedream50Pro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputSeedream50Pro

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_seedream50pro(
                input=InputSeedream50Pro(
                    prompt="prompt",
                    aspect_ratio="1:1",
                    resolution="1080p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_seedream50pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_sora2pro(
        self,
        *,
        input: InputSora2Pro,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        For complex, narrative-driven videos with remarkable consistency and realistic character-world interaction.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputSora2Pro

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputSora2Pro

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_sora2pro(
                input=InputSora2Pro(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    resolution="720p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_sora2pro(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_topaz_image_upscaler(
        self,
        *,
        input: InputTopazImageUpscaler,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Use the powerful and accurate Topaz image enhancer to upscale and enhance your images.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazImageUpscaler

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputTopazImageUpscaler,
            InputTopazImageUpscalerSourceImage_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_topaz_image_upscaler(
                input=InputTopazImageUpscaler(
                    source_image=InputTopazImageUpscalerSourceImage_Url(
                        url="url",
                    ),
                    target_resolution="1080p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_topaz_image_upscaler(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_topaz_image_upscaler_wonder(
        self,
        *,
        input: InputTopazImageUpscalerWonder,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Generative upscaling with realistic detail, precise text, and clean graphics — Topaz's highest-quality image upscaler.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazImageUpscalerWonder

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputTopazImageUpscalerWonder,
            InputTopazImageUpscalerWonderSourceImage_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_topaz_image_upscaler_wonder(
                input=InputTopazImageUpscalerWonder(
                    source_image=InputTopazImageUpscalerWonderSourceImage_Url(
                        url="url",
                    ),
                    target_resolution="1080p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_topaz_image_upscaler_wonder(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_topaz_video_upscaler(
        self,
        *,
        input: InputTopazVideoUpscaler,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Precision upscaling that cleans compression and noise while staying faithful to the source.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazVideoUpscaler

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputTopazVideoUpscaler,
            InputTopazVideoUpscalerSourceVideo_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_topaz_video_upscaler(
                input=InputTopazVideoUpscaler(
                    source_video=InputTopazVideoUpscalerSourceVideo_Url(
                        url="url",
                    ),
                    resolution="1080p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_topaz_video_upscaler(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_topaz_video_upscaler_hyperion25(
        self,
        *,
        input: InputTopazVideoUpscalerHyperion25,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Convert SDR video to 10-bit HDR with richer highlights, color, and tonal separation. The output keeps the source resolution.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazVideoUpscalerHyperion25

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputTopazVideoUpscalerHyperion25,
            InputTopazVideoUpscalerHyperion25SourceVideo_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_topaz_video_upscaler_hyperion25(
                input=InputTopazVideoUpscalerHyperion25(
                    source_video=InputTopazVideoUpscalerHyperion25SourceVideo_Url(
                        url="url",
                    ),
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_topaz_video_upscaler_hyperion25(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_topaz_video_upscaler_starlight_fast(
        self,
        *,
        input: InputTopazVideoUpscalerStarlightFast,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Faster generative diffusion upscaling at half the cost of Starlight Precise.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazVideoUpscalerStarlightFast

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputTopazVideoUpscalerStarlightFast,
            InputTopazVideoUpscalerStarlightFastSourceVideo_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_topaz_video_upscaler_starlight_fast(
                input=InputTopazVideoUpscalerStarlightFast(
                    source_video=InputTopazVideoUpscalerStarlightFastSourceVideo_Url(
                        url="url",
                    ),
                    resolution="1080p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_topaz_video_upscaler_starlight_fast(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_topaz_video_upscaler_starlight_hq(
        self,
        *,
        input: InputTopazVideoUpscalerStarlightHq,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Generative diffusion upscaling balancing detail and sharpness for medium-to-high quality sources.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazVideoUpscalerStarlightHq

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputTopazVideoUpscalerStarlightHq,
            InputTopazVideoUpscalerStarlightHqSourceVideo_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_topaz_video_upscaler_starlight_hq(
                input=InputTopazVideoUpscalerStarlightHq(
                    source_video=InputTopazVideoUpscalerStarlightHqSourceVideo_Url(
                        url="url",
                    ),
                    resolution="1080p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_topaz_video_upscaler_starlight_hq(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_topaz_video_upscaler_starlight_precise(
        self,
        *,
        input: InputTopazVideoUpscalerStarlightPrecise,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Generative diffusion upscaling for AI-generated and archival video with realistic faces, textures, and text.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputTopazVideoUpscalerStarlightPrecise

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputTopazVideoUpscalerStarlightPrecise,
            InputTopazVideoUpscalerStarlightPreciseSourceVideo_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_topaz_video_upscaler_starlight_precise(
                input=InputTopazVideoUpscalerStarlightPrecise(
                    source_video=InputTopazVideoUpscalerStarlightPreciseSourceVideo_Url(
                        url="url",
                    ),
                    resolution="1080p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_topaz_video_upscaler_starlight_precise(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_veed_fabric10(
        self,
        *,
        input: InputVeedFabric10,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Talking video with natural lip-sync and expressive animation.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputVeedFabric10

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputVeedFabric10,
            InputVeedFabric10Audio_Url,
            InputVeedFabric10StartImage_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_veed_fabric10(
                input=InputVeedFabric10(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    resolution="480p",
                    start_image=InputVeedFabric10StartImage_Url(
                        url="url",
                    ),
                    audio=InputVeedFabric10Audio_Url(
                        url="url",
                    ),
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_veed_fabric10(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_veed_video_background_removal(
        self,
        *,
        input: InputVeedVideoBackgroundRemoval,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Remove a video's background and return transparent WebM.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputVeedVideoBackgroundRemoval

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputVeedVideoBackgroundRemoval,
            InputVeedVideoBackgroundRemovalSourceVideo_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_veed_video_background_removal(
                input=InputVeedVideoBackgroundRemoval(
                    source_video=InputVeedVideoBackgroundRemovalSourceVideo_Url(
                        url="url",
                    ),
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_veed_video_background_removal(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_veo2(
        self,
        *,
        input: InputVeo2,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Google's earlier cinematic generator, kept for existing workflows.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputVeo2

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputVeo2

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_veo2(
                input=InputVeo2(
                    prompt="prompt",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_veo2(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_veo3(
        self,
        *,
        input: InputVeo3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Hollywood-grade, cinematic video straight from text—your go-to for hero campaigns.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputVeo3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputVeo3

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_veo3(
                input=InputVeo3(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    resolution="720p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_veo3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_veo31(
        self,
        *,
        input: InputVeo31,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        For unparalleled detail and nuance, perfect for when your vision requires the best possible quality.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputVeo31

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputVeo31

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_veo31(
                input=InputVeo31(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    resolution="720p",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_veo31(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_vidu_q3(
        self,
        *,
        input: InputViduQ3,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        The longest clips in the catalog — up to 16 seconds with native dialogue and sound, from a text prompt, from a start frame, or between a start and end frame

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputViduQ3

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputViduQ3

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_vidu_q3(
                input=InputViduQ3(
                    prompt="prompt",
                    resolution="540p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_vidu_q3(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_vidu_q3reference(
        self,
        *,
        input: InputViduQ3Reference,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Keep up to four subjects consistent across a clip from reference images.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputViduQ3Reference

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import (
            AsyncHedra,
            InputViduQ3Reference,
            InputViduQ3ReferenceImagesItem_Url,
        )

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_vidu_q3reference(
                input=InputViduQ3Reference(
                    prompt="prompt",
                    aspect_ratio="16:9",
                    resolution="540p",
                    duration_ms=1,
                    images=[
                        InputViduQ3ReferenceImagesItem_Url(
                            url="url",
                        )
                    ],
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_vidu_q3reference(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_wan27(
        self,
        *,
        input: InputWan27,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Wan 2.7 video with native audio — from a text prompt, from a first frame with an optional last frame, or from reference images that keep subjects consistent

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputWan27

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputWan27

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_wan27(
                input=InputWan27(
                    prompt="prompt",
                    resolution="720p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_wan27(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit_wan30(
        self,
        *,
        input: InputWan30,
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Wan 3.0 video with native audio, up to 30 seconds in one shot — from a text prompt, from a first frame with an optional last frame, or from reference images that keep subjects consistent

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        input : InputWan30

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra, InputWan30

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit_wan30(
                input=InputWan30(
                    prompt="prompt",
                    aspect_ratio="adaptive",
                    resolution="480p",
                    duration_ms=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_wan30(
            input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def submit(
        self,
        model: str,
        *,
        input: typing.Dict[str, typing.Any],
        webhook: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitResponse:
        """
        Runs any model in the catalog by its public id, with `input` passed through untyped — the same call the typed operations below make, minus the compile-time schema.

        Reach for it when the model is not known ahead of time: a client generated before a model shipped can still run it, and an id read from `GET /v3/models` at runtime needs no regeneration. Prefer the typed operation whenever your client already has one — `input` here is validated against the same published schema (`GET /v3/models/{model}`), so a bad field is a `400` at submit rather than an error before the call.

        Submits an asynchronous job and returns `202` with a job id. Fetch the result at `GET /v3/jobs/{job_id}` — each item in its `outputs[]` follows the `OutputItem` schema — or track progress via `GET /v3/jobs/{job_id}/status` / the SSE stream at `GET /v3/jobs/{job_id}/stream`.

        Parameters
        ----------
        model : str
            The model's public id (`GET /v3/models`).

        input : typing.Dict[str, typing.Any]
            Model-specific inputs, validated at submit against the model's published input schema (`GET /v3/models/{model}`).

        webhook : typing.Optional[str]
            URL to receive a signed completion webhook.

        idempotency_key : typing.Optional[str]
            Replays the original ack for a retried submit instead of enqueueing a duplicate job.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitResponse
            Accepted. The job runs asynchronously; poll `status_url` / `result_url` from the ack.

        Examples
        --------
        import asyncio

        from hedra import AsyncHedra

        client = AsyncHedra(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.jobs.submit(
                model="model",
                input={"key": "value"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit(
            model, input=input, webhook=webhook, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data
